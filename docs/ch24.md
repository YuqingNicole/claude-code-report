# 第24章 · 隐藏命令完整清单

## 24.1 命令总览
Claude Code的命令系统包含103个命令目录，涵盖：
基础命令：clear、exit、help、usage、status、version
会话管理：resume、session、rename、tag、summary
代码工具：review、brief、commit、commit-push-pr、diff、advisor
AI功能：agents、tasks、memory、thinking（thinkback、thinkback-play）
开发工具：doctor、debug-tool-call、insights、issue、bughunter
集成：bridge、chrome、desktop、remote-env、mobile、oauth-refresh
插件系统：plugin、reload-plugins、mcp、skills、hooks
配置：config、privacy-settings、permissions、keybindings、theme、color
高级功能：teleport、smooth-streaming、custom MCP 、remote-setup
测试/内部：ant-trace、break-cache、mock-limits、heapdump、perf-issue
## 24.2 ANT-Only内部命令
以下命令仅供Anthropic内部使用：
bridge-kick — Bridge故障注入测试工具：
```typescript
/bridge-kickclose1002 — 模拟WebSocket 关闭 /bridge-kickpoll404  —  下次轮询返回404/bridge-kick
registerfail — 注册瞬态失败 /bridge-kick  reconnect  —  直接触发重连 /bridge-kick  status  —
打印Bridge 状态
ant-trace — 内部追踪系统（103个文件） heapdump — 堆内存快照（用于内存泄漏调试） mock-limits —
模拟策略限制（测试企业功能） perf-issue — 性能问题分析 debug-tool-call — 工具调用调试
##24.3 已禁用命令考古
部分命令在源码中存在但已被禁用：
• good-claude — 可能是一个正面反馈机制（"GoodClaude!"）
• teleport — 会话传送功能（将会话从一个环境移动到另一个）
• ctx_viz — 上下文可视化工具（图形化展示上下文窗口的使用情况）
这些命令的存在暗示了Anthropic正在探索但尚未公开的功能方向。
##24.4 趣味命令

• stickers — 贴纸系统
• passes — 内部通行证系统
• thinkback/thinkback-play — 思维回放：回放Claude的思考过程
##24.5 高级命令
• ultraplan — 超级规划模式（更深度的任务分析和规划）
• bughunter — Bug猎人（自动化Bug搜索）
• voice — 语音模式
• fast — 快速模式（可能使用更轻量的模型）
• effort — 努力级别控制
• vim — Vim模式（启用Vim键绑定）
```